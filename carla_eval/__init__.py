"""CARLA-side evaluation for SDformerFlow and STTFlowNet.

Named `carla_eval` rather than `carla` on purpose: the CARLA client library imports as
`carla`, and a sibling package of that name on sys.path shadows it.
"""
